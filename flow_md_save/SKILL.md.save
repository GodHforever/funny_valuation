---
name: financial-statement-based-workflow
description: "A股财报驱动的全链路投研工作流。从财报PDF下载、结构化提取、七层深度研判、行情数据采集到多模型估值分析的端到端自动化流程。五阶段递进：下载财报 → 提取财报内容 → 深度分析研判 → 采集行情数据 → 交叉估值。每个阶段的产出作为下一阶段的输入，最终形成完整的投研报告。当用户需要对A股上市公司进行完整的基本面研究和估值分析时使用此workflow。"
---

# Financial Statement Based Workflow — A股财报驱动全链路投研

## 定位

这是一个**编排层 skill**，串联项目内三个子 skill 和全局辅助 skill，实现从原始财报到估值结论的端到端自动化投研流程。

> 核心理念：让数据说话，让分析有据，让估值可验证。每一步的产出都是下一步的输入，形成完整的证据链。

## 工作流总览

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Stage 1    │    │  Stage 2    │    │  Stage 3    │    │  Stage 4    │    │  Stage 5    │
│  下载财报   │───▶│  提取内容   │───▶│  深度分析   │───▶│  行情数据   │───▶│  估值分析   │
│             │    │             │    │             │    │             │    │             │
│ earnings-   │    │ earnings-   │    │ earnings-   │    │ china-      │    │ stock-      │
│ report-     │    │ report-     │    │ insight     │    │ earnings-   │    │ valuation   │
│ extractor   │    │ extractor   │    │             │    │ lite +      │    │             │
│ (download)  │    │ (extract)   │    │             │    │ macro-lite  │    │             │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
      │                  │                  │                  │                  │
      ▼                  ▼                  ▼                  ▼                  ▼
  data/             data/              data/              data/              data/
  earnings-pdf/     earnings-          earnings-          market-data/       valuation-
                    extracted/         analysis/                             reports/
```

## 前置条件

- **Python 3.6+**
- **pdfplumber**（Stage 2 提取 PDF 需要）

```bash
pip install pdfplumber
```

## 使用方式

### 快速开始（一句话触发）

```
请对 {股票代码} 执行完整的财报投研分析
```

```
请按照 financial-statement-based-workflow 分析 {股票代码}
```

### 带参数触发

```
请对 300014 执行完整投研分析，使用 2024 年年报，提取全部章节
```

## 输入参数

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| 股票代码 | 6位A股代码 | **必填** | 300014 |
| 报告年份 | 财报年份 | 最新可用 | 2024 |
| 报告类型 | annual/half/q1/q3 | annual | annual |
| 提取范围 | P1+P2 / 全部 / 指定章节 | P1+P2 | mda,risk,audit |

## 五阶段详细流程

---

### Stage 1：下载财报 PDF

**目标：** 从巨潮资讯下载目标公司的财报 PDF 原文

**使用 skill：** `earnings-report-extractor`（下载功能）

**命令：**
```bash
python .claude/skills/financial-statement-based-workflow/earnings-report-extractor/scripts/report_extractor.py \
    --code {股票代码} \
    --year {年份} \
    --report-type {报告类型} \
    --output-dir data/earnings-pdf/ \
    --list-sections
```

> 先用 `--list-sections` 查看章节结构，确认 PDF 下载成功。

**产出：**
- `data/earnings-pdf/{code}_*.pdf` — 财报 PDF 原文

**检查点：**
- 确认 PDF 文件已下载且大小合理（年报通常 > 1MB）
- 如下载失败，提示用户检查股票代码或网络连接

---

### Stage 2：提取财报内容

**目标：** 将 PDF 转化为结构化 Markdown 文本，提取关键章节

**使用 skill：** `earnings-report-extractor`（提取功能）

**命令：**
```bash
python .claude/skills/financial-statement-based-workflow/earnings-report-extractor/scripts/report_extractor.py \
    --pdf data/earnings-pdf/{下载的PDF文件名} \
    --output-dir data/earnings-extracted/{code}_{year}/ \
    --sections mda,risk,chairman_letter,business,audit,company_intro,important_matters
```

> 默认提取 P1 + P2 核心章节。如需全部章节，使用 `--all-sections`。

**产出：**
- `data/earnings-extracted/{code}_{year}/{code}_第X节_章节名.md` — 各章节完整文本
- `data/earnings-extracted/{code}_{year}/{code}_提取汇总.md` — 提取索引
- `data/earnings-extracted/{code}_{year}/{code}_structure.json` — 结构化章节树

**检查点：**
- 确认管理层讨论与分析（MD&A）已成功提取（这是后续分析的核心输入）
- 确认提取汇总中各章节字符数合理
- 如部分章节提取失败（退出码 1），评估是否影响后续分析

---

### Stage 3：深度分析研判

**目标：** 基于提取的财报文本 + 自动获取的财务数据，执行七层递进分析

**使用 skill：** `earnings-insight`

**步骤 3.1 — 获取结构化财务数据：**
```bash
python .claude/skills/financial-statement-based-workflow/earnings-insight/scripts/data_fetcher.py \
    --code {股票代码} \
    --output-dir data/earnings-analysis/{code}_{year}/
```

**步骤 3.2 — 执行七层分析：**

读取以下输入，按 `earnings-insight` 的七层分析框架生成研判报告：

| 输入来源 | 用途 |
|---------|------|
| `data/earnings-extracted/{code}_{year}/` 目录 | 非财务文本（MD&A、风险、业务描述等） |
| `data/earnings-analysis/{code}_{year}/` 数据文件 | 结构化财务数据 |

**分析框架（七层递进）：**
1. 财务数据深度拆解（收入质量、利润质量、资产负债、现金流）
2. 业务表述批判性解读（语言与行动一致性、被淡化的问题）
3. 行业与竞争格局（周期定位、竞争坐标）
4. 交叉验证（信号分类、矛盾识别）
5. 前瞻研判（领先指标、三情景、增长可持续性、风险前瞻、战略可执行性）
6. 结构化结论（核心判断 + 信号可信度 + 证伪条件）
7. 跟踪检查清单（可验证的监控指标）

**产出：**
- `data/earnings-analysis/{code}_{year}/{code}_{公司名}_深度研判报告.md` — 完整七层分析报告
- `data/earnings-analysis/{code}_{year}/*.json` — 结构化数据文件

**检查点：**
- 确认三情景分析概率之和为 100%
- 确认每个核心结论都标注了信号可信度
- 确认跟踪清单包含具体的量化阈值

---

### Stage 4：获取行情与市场数据

**目标：** 获取当前实时行情、财务指标、行业对比、宏观环境数据，为估值提供输入

**使用 skill：** `china-earnings-lite` + `china-macro-lite`

**步骤 4.1 — 公司财务数据与行业对比：**
```bash
python ~/.claude/skills/china-earnings-lite/scripts/earnings_lite.py \
    --code {股票代码} \
    --mode report \
    --periods 8 \
    --output-dir data/market-data/{code}/
```

**步骤 4.2 — 宏观经济数据（并行执行）：**
```bash
# 利率数据
python ~/.claude/skills/china-macro-lite/scripts/macro_lite.py \
    --category 利率 \
    --output-dir data/market-data/{code}/

# 通胀数据（CPI/PPI）
python ~/.claude/skills/china-macro-lite/scripts/macro_lite.py \
    --category 通胀 \
    --output-dir data/market-data/{code}/

# 货币供应（M1/M2，如该类别可用）
python ~/.claude/skills/china-macro-lite/scripts/macro_lite.py \
    --category 货币 \
    --output-dir data/market-data/{code}/
```

> 注：宏观数据获取失败不阻塞流程，Stage 5 会使用默认假设。

**产出：**
- `data/market-data/{code}/{code}_*.json` — 财务数据（利润表、资产负债表、现金流量表）
- `data/market-data/{code}/{code}_analysis.json` — 结构化分析结果
- `data/market-data/{code}/{code}_industry.json` — 行业对比数据
- `data/market-data/{code}/{code}_{公司名}_综合分析报告.md` — 综合分析报告
- `data/market-data/{code}/macro_*.json` — 宏观数据

**检查点：**
- 确认实时行情数据获取成功（当前股价、PE、PB 等）
- 确认行业对比数据可用
- 如宏观数据获取失败，不阻塞流程，在估值阶段使用默认假设

---

### Stage 5：估值分析

**目标：** 结合深度研判结论 + 财务数据 + 行情数据，执行多模型交叉验证估值

**使用 skill：** `stock-valuation`

**步骤 5.1 — 估值数据采集：**
```bash
python .claude/skills/financial-statement-based-workflow/stock-valuation/scripts/valuation_data.py \
    --code {股票代码} \
    --output-dir data/valuation-reports/{code}_{year}/
```

**步骤 5.2 — 数据预处理：**
```bash
python .claude/skills/financial-statement-based-workflow/stock-valuation/scripts/valuation_preprocessor.py \
    --data-file data/valuation-reports/{code}_{year}/{code}_valuation_data.json \
    --output-dir data/valuation-reports/{code}_{year}/ \
    --qualitative-doc data/earnings-analysis/{code}_{year}/{code}_{公司名}_深度研判报告.md
```

> `--qualitative-doc` 传入 Stage 3 的研判报告，启用**完整模式（模式A）**，获得最高置信度。

**步骤 5.3 — 模型计算：**
```bash
python .claude/skills/financial-statement-based-workflow/stock-valuation/scripts/valuation_models.py \
    --preprocessed-file data/valuation-reports/{code}_{year}/{code}_preprocessed.json \
    --output-dir data/valuation-reports/{code}_{year}/
```

**步骤 5.4 — 整合分析：**
```bash
python .claude/skills/financial-statement-based-workflow/stock-valuation/scripts/valuation_integrator.py \
    --data-file data/valuation-reports/{code}_{year}/{code}_valuation_data.json \
    --preprocessed-file data/valuation-reports/{code}_{year}/{code}_preprocessed.json \
    --model-results-file data/valuation-reports/{code}_{year}/{code}_model_results.json \
    --output-dir data/valuation-reports/{code}_{year}/
```

**步骤 5.5 — 报告生成：**
```bash
python .claude/skills/financial-statement-based-workflow/stock-valuation/scripts/valuation_report.py \
    --integrated-file data/valuation-reports/{code}_{year}/{code}_integrated.json \
    --data-file data/valuation-reports/{code}_{year}/{code}_valuation_data.json \
    --preprocessed-file data/valuation-reports/{code}_{year}/{code}_preprocessed.json \
    --model-results-file data/valuation-reports/{code}_{year}/{code}_model_results.json \
    --output-dir data/valuation-reports/{code}_{year}/
```

**产出：**
- `data/valuation-reports/{code}_{year}/{code}_{公司名}_估值分析报告.md` — 完整估值报告
- `data/valuation-reports/{code}_{year}/{code}_integrated.json` — 整合分析结果
- `data/valuation-reports/{code}_{year}/{code}_model_results.json` — 8模型计算结果

**检查点：**
- 确认至少 3 个以上估值模型产出有效结果
- 确认估值区间（悲观/基准/乐观）合理
- 确认估值陷阱检测已执行

---

## 最终交付物

完成全部五个阶段后，向用户呈现以下关键结论：

### 必须呈现的内容

1. **公司概况一句话**：{公司名}（{代码}），{行业}，当前股价 {价格} 元
2. **核心研判结论**：来自 Stage 3 第六层结构化结论
3. **估值结论**：
   - 加权综合估值区间（悲观 / 基准 / 乐观）
   - 当前价格所处区间（5级定位）
   - 安全边际百分比
   - 建议买入区间
4. **主要风险**：估值陷阱检测结果 + 前瞻风险
5. **跟踪清单**：下季度需验证的关键指标

### 报告文件索引

引导用户查看各阶段的详细报告文件路径。

---

## 数据单位约定

各 Stage 脚本统一使用以下单位，确保数据流兼容：

| 数据项 | 单位 | 说明 |
|--------|------|------|
| 股价 | 元 | push2 API 原始值需 /100 |
| PE/PB/PS | 倍 | push2 API 原始值需 /100 |
| 市值 | 亿元 | push2 API f116 以元为单位，需 /1e8 |
| 营收/利润 | 亿元 | 财务报表数据需 /1e8 |
| WACC/利率 | 小数 | 如 0.08 表示 8%，报告展示时自动转换 |

## 错误处理与降级策略

| 阶段 | 失败场景 | 降级方案 |
|------|---------|---------|
| Stage 1 | PDF 下载失败 | 提示用户手动下载并放入 `data/earnings-pdf/`，跳转 Stage 2 |
| Stage 2 | 部分章节提取失败 | 继续分析已提取章节，在 Stage 3 标注信息缺失 |
| Stage 3 | 财务数据获取失败 | 使用 Stage 4 的数据回填，或仅做定性分析 |
| Stage 4 | 行情数据获取失败 | 使用 Stage 3 已获取的数据，估值使用保守假设 |
| Stage 4 | 宏观数据获取失败 | 不阻塞流程，估值使用默认利率假设 |
| Stage 5 | 部分模型不可用 | 自动跳过，权重重新分配至可用模型 |

## 断点续跑

如果流程中断，可以从任意阶段重新开始：

```
请从 Stage {N} 继续分析 {股票代码}，之前的数据在 data/ 目录下
```

AI 会自动检测已有的中间产物，跳过已完成的步骤。

## 多公司对比

可以对多家公司分别执行完整流程后，请求横向对比：

```
请对比 {代码1} 和 {代码2} 的估值分析结果
```

## 免责声明

> 本工作流基于公开财务数据和年报信息，运用结构化分析框架生成，仅供研究参考，不构成任何投资建议。报告中的前瞻性研判基于特定假设条件，实际结果可能与预期存在重大差异。投资者应当独立判断，自行承担投资风险。
